apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-back-end-api-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-back-end-api-app
  template:
    metadata:
      labels:
        app: admin-back-end-api-app
        role: admin-back-end
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: admin-back-end-api-app
spec:
  selector:
    app: admin-back-end-api-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
